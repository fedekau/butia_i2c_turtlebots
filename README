######## ABOUT BUTIA GIT #########

Each folder contains a different project.
In each folder should be a makefile or script to compile
or the code itself in the case of python or lua code.

bobot: Concontains Bobot server, this is responsible for
    communicating with the firmware installed in the butia
    and export capabilities of the robot using TCP / IP
    requirements-proyects: serialcomm, lualibusb

turtle_art_plugin: Turtle Arts plugin, implements support of
    Butia in Turttle Arts. Using Turttle Arts plugin system.
    In this folder is a shell script to make an .xo whit 
    TurtleArtButia, a TurttleArt program whit logo changed
    and already instaled the butia plugin
    requirements-proyects: bobotXO

firmware: Contains the firmware butia for ardino Mega (for 
    the moment), also contains other firmwares and testings
    requirements-proyects: arduino, avrdude

usb4all: proyect usb4all, using protocol specifications

API: implemented API to comunicate whit bobot server

serialcomm: library for using serial

lualibusb: binding library for using libusb in lua
    requirements-proyects: libusb-dev

######### Other remotes proyects/sources in git #######

libs/lua: lua source
	requeriments: libreadline-dev

libs/libusb: libusb headers files



### Last edited 2011/06/12 by Guillermo Reisch <greisch@fing.edu.uy> ###
